# ADR-001: Evals as CI gates, not notebooks

## Context
Agent regressions are subtle and frequent.

## Decision
Eval suite runs in CI and fails the build below 100% on the golden set;
every failure links to its trace and is clustered by failure type.

## Consequences
- Golden set must be curated, not auto-generated.
- Flaky agents surface immediately instead of in production.
